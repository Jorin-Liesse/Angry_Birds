export class Ground {
  constructor(position, size) {
    this.canvas = document.getElementById("mainCanvas");
    this.position = position;
    this.size = size;
  }

  update(dt, inputs) {}

  draw() {}
}
